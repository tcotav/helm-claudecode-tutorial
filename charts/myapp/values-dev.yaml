# Dev environment overrides.
# Goal: low resource footprint, verbose logging, no external traffic.

replicaCount: 1

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 100m
    memory: 128Mi

ingress:
  enabled: false

config:
  logLevel: debug
  maxConnections: 10
